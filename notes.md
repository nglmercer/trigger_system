# actions required changes

- actions is do, then is alias for do [completed]

- set mode action (optional) default all // utility or decorator for custom set set mode
- on actions chain allways is group with default all
- rename actions node to type actions node // this is a selector for type and params
- add do (then alias) or else element,(actual exist event element, action and conditional (element|| group) node)
- when need especific inline conditions without actions or group actions its necesary use do element (implicit when exist actions but explicit if need inline conditions and not actions)
- 