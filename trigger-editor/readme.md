# notes

- implemented window methods to enable custom export, and custom import from yaml with filename or metadata, always use window.postMessage and window.addEventListener to communicate with the parent window
- this option is hidden by default but can be enabled with window.triggerEditorConfig or similar